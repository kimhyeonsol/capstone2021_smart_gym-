spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/capstone2021?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root
    initialization-mode : always

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    defer-datasource-initialization : true